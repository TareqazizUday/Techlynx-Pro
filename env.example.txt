# Django Settings
SECRET_KEY=your-secret-key-here-change-this-in-production
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,techlynxpro.com,www.techlynxpro.com,3.82.24.132
# Optional: absolute path to SQLite file (default: private/db.sqlite3, or legacy ./db.sqlite3 if it exists)
# SQLITE_PATH=/var/lib/techlynxpro/db.sqlite3
# HTTPS origins for CSRF (include scheme), comma-separated — required if users hit the site under multiple hostnames
CSRF_TRUSTED_ORIGINS=https://techlynxpro.com,https://www.techlynxpro.com
# Optional: non-default admin URL (must end with / in .env or it is added automatically)
# ADMIN_PATH=manage-your-site/

# Gemini API (Optional)
GEMINI_API_KEY=your-gemini-api-key-here
# Model id (e.g. gemini-2.0-flash, gemini-2.5-flash) — see Google AI Studio
# GEMINI_MODEL=gemini-2.0-flash

